type Query {
  getCountShortUrls: Int @function( name: "countAllShortUrls-${env}")
}

type ShortUrls @model @auth(rules: [{allow: public}]) {
  id: ID!
  destination: String!
  shortened: String!
  requests: Int
}
